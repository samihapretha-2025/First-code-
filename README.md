# First-code-
1.Write a C++ program that prints all the odd numbers in the range from 1 to 100 using a loop.  #include &lt;.iostream> #include &lt;conio.h> using namespace std;  int main() {  for (int i = 1; i &lt;= 100; i++) {         if (i % 2 != 0)         {         cout &lt;&lt; i &lt;&lt; " ";         }  } getch(); }
